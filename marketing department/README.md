# Marketing department — daily tracker

Frontend for the Express app at the repo root. Run `npm start` from the project folder, then open http://localhost:5500

PINs are stored as hashes on the server (`auth.js`), not in this folder.
